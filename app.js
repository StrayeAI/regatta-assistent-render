const $=id=>document.getElementById(id), R=6371000; let marks=[],active=0,pos=null,weather=null,lastFetch=0,line={pin:null,boat:null},deferredPrompt=null;let map=L.map('map').setView([59.205,10.79],13), layers=L.layerGroup().addTo(map), boatMarker, routeLine, layLines=[];L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OSM'}).addTo(map);if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{});window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;$('install').hidden=false});$('install').onclick=()=>deferredPrompt?.prompt();function rad(d){return d*Math.PI/180}function deg(r){return(r*180/Math.PI+360)%360}function norm(d){return(d%360+360)%360}function diff(a,b){return((a-b+540)%360)-180}function kt(ms){return(ms||0)*1.94384}function bearing(a,b,c,d){let p1=rad(a),p2=rad(c),dl=rad(d-b);return deg(Math.atan2(Math.sin(dl)*Math.cos(p2),Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl)))}function distance(a,b,c,d){let p1=rad(a),p2=rad(c),dp=rad(c-a),dl=rad(d-b),x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(x),Math.sqrt(1-x))}function dest(lat,lon,brg,m){let δ=m/R,θ=rad(brg),φ1=rad(lat),λ1=rad(lon);let φ2=Math.asin(Math.sin(φ1)*Math.cos(δ)+Math.cos(φ1)*Math.sin(δ)*Math.cos(θ));let λ2=λ1+Math.atan2(Math.sin(θ)*Math.sin(δ)*Math.cos(φ1),Math.cos(δ)-Math.sin(φ1)*Math.sin(φ2));return[deg(φ2),deg(λ2)]}function save(){localStorage.regattaV2=JSON.stringify({marks,active,line});render()}function load(){try{let s=JSON.parse(localStorage.regattaV2||'{}');marks=s.marks||[];active=s.active||0;line=s.line||line}catch{}render()}function render(){layers.clearLayers();layLines.forEach(l=>l.remove());layLines=[];$('marks').innerHTML=marks.map((m,i)=>`<tr><td>${i===active?'▶ ':''}${i+1}</td><td>${m.name}</td><td>${m.lat.toFixed(5)}, ${m.lon.toFixed(5)}</td><td><button onclick="active=${i};save();update()" class="secondary">Velg</button></td></tr>`).join('');marks.forEach((m,i)=>L.marker([m.lat,m.lon]).addTo(layers).bindPopup(`${i+1}. ${m.name}`));if(routeLine)routeLine.remove();if(marks.length)routeLine=L.polyline(marks.map(m=>[m.lat,m.lon]),{color:'#20a4f3',weight:3}).addTo(map);if(line.pin&&line.boat){L.polyline([[line.pin.lat,line.pin.lon],[line.boat.lat,line.boat.lon]],{color:'#ffd166',weight:4}).addTo(layers);$('startline').textContent=`Startlinje satt. Pinne ${line.pin.lat.toFixed(5)},${line.pin.lon.toFixed(5)} / Bøye ${line.boat.lat.toFixed(5)},${line.boat.lon.toFixed(5)}`}if(marks.length)map.fitBounds(L.latLngBounds(marks.map(m=>[m.lat,m.lon])),{padding:[25,25]})}async function fetchData(lat,lon){$('status').textContent='Henter vær/hav…';let wx=`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current=wind_speed_10m,wind_direction_10m,wind_gusts_10m&wind_speed_unit=ms&timezone=auto`;let sea=`https://marine-api.open-meteo.com/v1/marine?latitude=${lat}&longitude=${lon}&current=wave_height,wave_direction,wave_period,ocean_current_velocity,ocean_current_direction&timezone=auto`;let[w,m]=await Promise.all([fetch(wx).then(r=>r.json()),fetch(sea).then(r=>r.json())]);weather={wind:w.current,marine:m.current};$('status').textContent='Live'}function correction(brg){let m=weather?.marine||{};if(m.ocean_current_velocity==null||m.ocean_current_direction==null)return{corr:0,text:'ingen strømdata'};let side=m.ocean_current_velocity*Math.sin(rad(diff(m.ocean_current_direction,brg)));let corr=Math.max(-14,Math.min(14,deg(Math.atan2(side,3.0))));return{corr,text:`strøm ${m.ocean_current_velocity.toFixed(2)} m/s mot ${Math.round(m.ocean_current_direction)}°, komp ${corr.toFixed(0)}°`}}function route(brg){let w=weather.wind,from=w.wind_direction_10m,to=norm(from+180),twa=Math.abs(diff(brg,from)),up=+$('upwind').value||43,down=+$('downwind').value||150,cur=correction(brg),mode,cands;if(twa<up){mode='KRYSS';cands=[norm(from+up+cur.corr),norm(from-up+cur.corr)]}else if(twa>down){mode='LENS/JIBB';let a=180-down;cands=[norm(to+a+cur.corr),norm(to-a+cur.corr)]}else{mode='DIREKTE/SLØR';cands=[norm(brg+cur.corr)]}cands=cands.map(c=>({course:c,err:Math.abs(diff(c,brg))})).sort((a,b)=>a.err-b.err);return{mode,best:cands[0].course,alt:cands[1]?.course,twa,cur}}function update(){if(!pos||!weather)return;if(boatMarker)boatMarker.setLatLng([pos.lat,pos.lon]);else boatMarker=L.circleMarker([pos.lat,pos.lon],{radius:8,color:'#06d6a0',fillOpacity:1}).addTo(map).bindPopup('Båt');let w=weather.wind,m=weather.marine;$('wind').textContent=`${w.wind_speed_10m.toFixed(1)} m/s fra ${Math.round(w.wind_direction_10m)}°`;$('sea').textContent=`${m.wave_height??'–'} m / ${m.ocean_current_velocity??'–'} m/s`;if(active>=marks.length){$('leg').textContent='Ferdig';return}let t=marks[active],brg=bearing(pos.lat,pos.lon,t.lat,t.lon),d=distance(pos.lat,pos.lon,t.lat,t.lon);if(d<(+$('radius').value||60)&&active<marks.length-1){active++;save();return update()}let r=route(brg);$('leg').textContent=`${active+1}: ${t.name} (${Math.round(d)} m)`;$('course').textContent=`${Math.round(r.best)}°`;$('advice').textContent=`${r.mode}: styr ca. ${Math.round(r.best)}° mot ${t.name}${r.alt?`, alternativ ${Math.round(r.alt)}°`:''}.`;$('details').textContent=`Peiling ${Math.round(brg)}°, TWA ${Math.round(r.twa)}°, ${r.cur.text}. SOG ${kt(pos.sog).toFixed(1)} kn / COG ${Math.round(pos.cog||0)}°.`;layLines.forEach(l=>l.remove());layLines=[];[r.best,r.alt].filter(Boolean).forEach(c=>{let p=dest(pos.lat,pos.lon,c,2500);layLines.push(L.polyline([[pos.lat,pos.lon],p],{color:'#ffd166',className:'layline'}).addTo(map))});if(line.pin&&line.boat){let lb=bearing(line.pin.lat,line.pin.lon,line.boat.lat,line.boat.lon),fav=diff(w.wind_direction_10m,lb)>0?'bøye':'pinne';let dl=Math.min(distance(pos.lat,pos.lon,line.pin.lat,line.pin.lon),distance(pos.lat,pos.lon,line.boat.lat,line.boat.lon));$('startline').textContent=`Favored end: ${fav}. Nærmeste linje-end ca. ${Math.round(dl)} m.`}}$('start').onclick=()=>navigator.geolocation.watchPosition(async p=>{pos={lat:p.coords.latitude,lon:p.coords.longitude,sog:p.coords.speed||0,cog:p.coords.heading||0};map.setView([pos.lat,pos.lon],map.getZoom());if(Date.now()-lastFetch>300000){lastFetch=Date.now();try{await fetchData(pos.lat,pos.lon)}catch(e){$('status').textContent='Værfeil';$('advice').textContent='Klarte ikke hente vær/havdata: '+e.message}}update()},{enableHighAccuracy:true,maximumAge:1200,timeout:10000});$('addMark').onclick=()=>{let lat=+$('mlat').value,lon=+$('mlon').value;if(!lat||!lon)return alert('Mangler lat/lon');marks.push({name:$('mname').value||`Merke ${marks.length+1}`,lat,lon});save()};$('useHere').onclick=()=>{if(!pos)return alert('Start GPS først');$('mlat').value=pos.lat.toFixed(6);$('mlon').value=pos.lon.toFixed(6)};$('clear').onclick=()=>confirm('Tømme bane?')&&(marks=[],active=0,save());$('sample').onclick=()=>{marks=[{name:'Start Hankø',lat:59.2035,lon:10.7700},{name:'Toppmerke',lat:59.2245,lon:10.7920},{name:'Offset',lat:59.2260,lon:10.7970},{name:'Bunnmerke',lat:59.1980,lon:10.7620},{name:'Mål',lat:59.2050,lon:10.7730}];active=0;save()};$('setPin').onclick=()=>{if(!pos)return alert('Start GPS først');line.pin={lat:pos.lat,lon:pos.lon};save()};$('setBoat').onclick=()=>{if(!pos)return alert('Start GPS først');line.boat={lat:pos.lat,lon:pos.lon};save()};load();
